# Neuron — Basic Example

This example demonstrates what a single neuron computes.

A neuron:
- multiplies inputs by weights
- sums the results
- adds a bias

## How to Run

```bash
python basic_neuron.py
